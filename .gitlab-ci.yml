stages:
  - build
  - test

before_script:
  - rustup update
  - apt update && apt upgrade -y && apt install -y fuse

rust-latest:
  stage: build
  image: rust:latest
  script:
    - cargo build --verbose
    - cargo test --verbose

rust-nightly:
  stage: build
  image: rustlang/rust:nightly
  script:
    - cargo build --verbose
    - cargo test --verbose
  allow_failure: true

test-suite:
  stage: test
  image: rust:latest
  script:
    - cargo build --verbose
    - sh test-suite/all.sh
